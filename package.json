{
  "name": "laju-next",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "server": "npx tsx --watch server.ts",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json --watch",
    "generate": "drizzle-kit generate:sqlite",
    "migrate": "npx tsx services/migrator.ts"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^2.0.0",
    "@sveltejs/kit": "^1.27.4",
    "autoprefixer": "^10.4.16",
    "drizzle-kit": "^0.20.2",
    "postcss": "^8.4.31",
    "svelte": "^4.0.5",
    "svelte-check": "^3.6.0",
    "tailwindcss": "^3.3.5",
    "typescript": "^5.0.0",
    "vite": "^4.4.2"
  },
  "type": "module",
  "dependencies": {
    "better-sqlite3": "^9.1.1",
    "drizzle-orm": "^0.29.0",
    "hyper-express": "^6.14.0"
  }
}
